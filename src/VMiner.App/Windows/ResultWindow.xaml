<Window x:Class="VMiner.Windows.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:VMiner.Controls"
        Title="VMiner — Results" Width="860" Height="600"
        MinWidth="620" MinHeight="430"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="VocabularyTokenStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="Margin" Value="1" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="TokenBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TokenBorder" Property="Background"
                                        Value="{StaticResource SelectionHoverBrush}" />
                                <Setter TargetName="TokenBorder" Property="BorderBrush"
                                        Value="{StaticResource AccentBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="TokenBorder" Property="Background"
                                        Value="{StaticResource SelectionBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="TokenBorder" Property="BorderBrush"
                                        Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                <Setter Property="Opacity" Value="0.82" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsInCollection}" Value="True">
                    <Setter Property="Background" Value="{StaticResource SavedBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource SavedBorderBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="VocabularyTokenTemplate">
            <Button Style="{StaticResource VocabularyTokenStyle}"
                    IsEnabled="{Binding IsVocabulary}"
                    Tag="{Binding}" ToolTip="{Binding TooltipText}"
                    Click="VocabularyTokenClicked">
                <StackPanel>
                    <TextBlock Text="{Binding DisplayReading}" MinHeight="14"
                               HorizontalAlignment="Center" FontSize="10"
                               Foreground="{StaticResource ReadingBrush}" FontFamily="Yu Gothic UI" />
                    <TextBlock Text="{Binding Surface}" HorizontalAlignment="Center"
                               FontSize="20" FontFamily="Yu Gothic UI" />
                </StackPanel>
            </Button>
        </DataTemplate>
    </Window.Resources>
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="14,10"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Copy Japanese" Click="CopyJapanese" Margin="0,0,7,0" />
                    <Button Content="Copy reading" Click="CopyReading" Margin="0,0,7,0" />
                    <Button Content="Copy English" Click="CopyTranslation" Margin="0,0,12,0" />
                    <CheckBox x:Name="PinCheck" Content="Always on top"
                              Checked="ApplyPin" Unchecked="ApplyPin" />
                </StackPanel>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center"
                           Foreground="{StaticResource MutedBrush}" Text="Ready" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Border Margin="16" Padding="18" CornerRadius="10"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="1" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,5">
                    <TextBlock DockPanel.Dock="Right" Text="Click a word to add it to your collection"
                               FontSize="11" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="JAPANESE VOCABULARY" FontSize="11"
                               FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}" />
                </DockPanel>
                <ScrollViewer Grid.Row="1" MaxHeight="250"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="VocabularyTokens"
                                  ItemTemplate="{StaticResource VocabularyTokenTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock Grid.Row="2" Text="READING" Margin="0,6,0,5" FontSize="11"
                           FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}" />
                <TextBlock x:Name="ReadingText" Grid.Row="3" Margin="0,0,0,14"
                           Foreground="{StaticResource WarmTextBrush}" TextWrapping="Wrap"
                           FontFamily="Yu Gothic UI" />

                <Border Grid.Row="4" Background="{StaticResource BorderBrush}" />

                <TextBlock Grid.Row="5" Text="ENGLISH TRANSLATION" Margin="0,14,0,7"
                           FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}" />
                <TextBox x:Name="TranslationText" Grid.Row="6" Padding="0"
                         IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" Background="Transparent"
                         BorderThickness="0" />
                </Grid>
            </Border>

            <Border x:Name="LoadingOverlay" Margin="16" CornerRadius="10"
                    Background="{StaticResource OverlayBrush}" Visibility="Collapsed">
                <StackPanel Width="280" HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <controls:MiningAnimation x:Name="MiningAnimation"
                                              Width="118" Height="121"
                                              Margin="0,0,0,12"
                                              Visibility="Collapsed" />
                    <TextBlock x:Name="LoadingText" Text="Mining…"
                               HorizontalAlignment="Center" FontSize="15"
                               FontWeight="SemiBold" Foreground="{StaticResource ForegroundBrush}" />
                    <ProgressBar Margin="0,16,0,0" Height="5"
                                 IsIndeterminate="True"
                                 Background="{StaticResource PanelRaisedBrush}"
                                 Foreground="{StaticResource AccentBrush}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
