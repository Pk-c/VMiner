<Window x:Class="VMiner.Windows.AddVocabularyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add to collection" Width="590" Height="570"
        MinWidth="520" MinHeight="520"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="ADD TO COLLECTION" FontSize="12" FontWeight="SemiBold"
                   Foreground="{StaticResource MutedBrush}" />

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Label Content="Word / dictionary form" />
                <TextBox x:Name="WordBox" FontFamily="Yu Gothic UI" FontSize="18" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <Label Content="Reading" />
                <TextBox x:Name="ReadingBox" FontFamily="Yu Gothic UI" FontSize="18" />
            </StackPanel>
        </Grid>

        <Label Grid.Row="2" Content="English definition" Margin="0,15,0,5" />
        <TextBox x:Name="DefinitionBox" Grid.Row="3" MinHeight="66"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" />

        <TextBlock x:Name="DefinitionStatus" Grid.Row="4" Margin="0,7,0,0"
                   FontSize="12" Foreground="{StaticResource AccentBrush}" />

        <Label Grid.Row="5" Content="Sentence example" Margin="0,15,0,5" />
        <Grid Grid.Row="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="8" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBox x:Name="JapaneseSentenceBox" IsReadOnly="True"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" FontFamily="Yu Gothic UI" />
            <TextBox x:Name="EnglishSentenceBox" Grid.Row="2" IsReadOnly="True"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" />
        </Grid>

        <Grid Grid.Row="7" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SaveStatus" VerticalAlignment="Center"
                       Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Cancel" Click="CancelClicked" Margin="0,0,9,0" />
                <Button x:Name="AddButton" Content="Add word" Click="AddClicked"
                        Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
